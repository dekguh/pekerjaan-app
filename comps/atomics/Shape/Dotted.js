const Dotted = ({ classes, ...rest }) => {
    return (
        <img
            src='/images/dotted.png'
            {...rest}
            className={classes}
        />
    )
}

export default Dotted
